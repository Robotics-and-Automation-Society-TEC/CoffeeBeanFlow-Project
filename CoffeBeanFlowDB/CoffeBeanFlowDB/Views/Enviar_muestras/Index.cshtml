@model IEnumerable<CoffeBeanFlowDB.Models.Enviar_muestrasItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID_Catacion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID_Catacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Trilla">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Trilla">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Trilla">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
